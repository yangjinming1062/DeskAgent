# 伙伴层交互设计

> 桌面伙伴（companion）的交互设计描述：形象资产体系、动画状态机、伙伴生命周期、onboarding、陪伴交互范式、语音策略、故障态。
> 这是**描述性文档**——只记产品设计意图与体验原则，不出现字段名、接口方法名、配置键、供应商名等代码相关内容。协议契约见 [PROTOCOL.md](PROTOCOL.md)；架构动机见 [ARCHITECTURE.md](ARCHITECTURE.md)；实现细节见各模块 [README.md](README.md)。

## 设计哲学

核心定位是为用户提供**情绪价值**，一切交互取舍服从于此：

- **以游戏设计思维做交互**：每次点击、每次状态切换都应有"有意思"的反馈，而非冷冰冰的功能响应。点击不是"触发回调"，是"戳了一下活的东西"。
- **永远不要完全静态**：静态贴图承载不了情感。形象必须时刻"活着"——呼吸、微动作、反应。用户不操作时它也得自己动。
- **陪伴叙事优于工具叙事**：所有加载、等待、错误都以"伙伴正在做某事"呈现，不暴露技术过程。"生成形象中"是"它正在想自己该长什么样"，不是 spinner。
- **表达永不空白与渐进式定制**：头像确认后，无 3D 模型的窗口期（生成中/失败/无 key/余额耗尽/换模空挡）由**静态精灵相册**兜底——用户看到的始终是自己的角色，程序化蛋形仅存在于头像产生之前的引导期与相册完全不可用的绝对兜底；换装系统（材质预设与 AI 纹理）支持后台热替而无需重新生成模型。
- **个性化驱动，非模板化**：伙伴的动作与反应从其**角色定义 + 对用户的记忆**派生。角色定义（静态、用户定义）决定"长什么样、性格如何"——驱动形象与动画资产生成；记忆（动态、随互动累积）决定"此刻怎么表现"——驱动运行时言语、情绪、主动频率。

---

## 1. 形象资产与图像生成体系

### 1.1 图像生成场景与资产总览

SpiritAgent 中的图像生成服务于角色数字身份的确立、实时情绪表达、3D 建模输入、无 3D 时的视觉降级以及外观定制换装。所有生成均紧密围绕用户的角色定义与身份锚点展开：

| 图像资产 / 场景 | 生成逻辑与特征 | 产品用途 |
|---|---|---|
| **头像种子图** | 基于角色描述 + 可选参考图（身份锚点与可选风格参考图），生成正面半身特写，确认后锁定不再变更 | 唯一锚定后续所有产品的角色形象；设置页展示与基础聊天头像；后续所有派生图的身份基准 |
| **头像情绪图** | 基于头像种子图 + 情绪神态 + 当前服装设定生成头部特写，按情绪标签懒加载生成（支持后台预热），自动色度抠图为透明背景 | 对话窗口中表达伙伴情绪；激活时替换左侧聊天头像，`neutral` 情绪直接复用基础头像；换装后着装自动同步 |
| **全身种子图** | 优先基于用户上传的参考图（无参考图时基于头像种子图）+ 角色描述与 3D 友好 A-pose 姿态限制；首次直接根据后端预设画风一次性并发生成多风格正面样图，用户选择其中一张后可直接确认或输入反馈微调重绘（微调时基于当前选中的参考图重新生成），确认正面后系统自动派生左/右/背面多视图 | 作为多视图输入提交图生 3D 管线，生成绑定骨骼的专属 3D 模型 |
| **全身精灵图** | 基于头像种子图 + 开放式情景/动作/情绪描述产生，自适应背景色并自动抠图为透明背景；相册管理并通过语义匹配优先复用已存图片（含专属 waiting 图） | 当 3D 模型不可用（生成中/失败/无 Key/换模空挡）时，伙伴在桌面常驻渲染的静态降级展示 |
| **贴图 / 材质** | 基于换装描述与体表展开规范，并发生成 5 通道 PBR 贴图（albedo 漫反射、normal 法线、roughness 粗糙度、metalness 金属度、displacement 置换） | 支持换装系统中的 AI 纹理热替、几何服装表面材质与挂件材质，改变角色外观而零模型重生 |
| **自主礼物创作** | 伙伴在夜间自主反思与创作中，根据互动记忆与画像自主设计新服装（并发生成 5 通道 PBR 贴图） | 存入装扮屋作为给用户的惊喜礼物盒，次日早晨主动提醒用户拆开 |
| **表情预热生成** | 当伙伴注册新自定义表情或夜间拓展表情库时，后台静默异步预热生成表情头像 | 提前就绪情绪图像资产，消除用户后续对话中的生成等待延迟 |
| **Agent 任务生图** | 伙伴作为智能体在日常对话或工作任务中响应用户的明确作图请求 | 满足用户日常绘图、设计、插画等生产力与创作需求（调用内置生图工具） |

伙伴的"身体"由 3D 模型提供，"穿什么"由换装层提供，"什么表情"由头像情绪图提供。切换状态 = 切换播放的骨骼动画（§2）；切换情绪 = 聊天窗表情头像换图；换装 = 热替材质/纹理或装配几何服装——三者正交组合，互不阻塞。**表情不走 3D 面部**：生成模型的脸部在桌面尺寸下精细度不足，面部变形目标承载不了可读的情绪表达，因此 3D 面部只保留眨眼与口型两个微信号，情绪的面部表达交给以头像种子图为基准生成的头像情绪图（按情绪标签缓存复用，未命中才生成；生成未就绪或失败时回退头像种子图，表达永不空白）。

**模型生成由云端单一路径支撑**：图生3D——以正面确认后自动派生的正/左/右/背多视图全身种子图为输入提交供应商生成绑定骨骼的 3D 模型，并注入面部变形目标（眨眼/口型）后下发。动画不内嵌模型——全部动画由客户端关键帧注入（动画与模型的事实规范见 [docs/MODEL_SPEC.md](docs/MODEL_SPEC.md)）。生成失败时客户端渲染程序化蛋形兜底角色。

### 1.2 渲染降级（永不空白）

**伙伴表达永不空白**（[ARCHITECTURE.md](ARCHITECTURE.md) 不变量），按三级级联兜底：

1. **GLB（满体验）**：3D 引擎实时渲染——骨骼动画覆盖全部状态（§2），面部带眨眼/口型微信号，无需等待逐状态生成。
2. **静态精灵相册**：头像确认后、GLB 不可用期间的降级渲染。相册由云端按需懒生成——以自由语义（状态/情绪/反应的开放式描述）在已有相册标签中匹配，命中即用，未命中则以头像为身份参考生成一张新的；每用户一张"等待/切换"图优先就绪。自适应色幕抠图转为透明 PNG，图间淡切 + 呼吸保持"活着"的观感；戳/拖/气泡/语音全部照常。
3. **程序化蛋形**：仅头像产生之前的引导期占位 + 相册完全不可用（后端不可达/全部生成被拒）时的绝对兜底（基本体组合 + 正弦驱动呼吸/眨眼/说话浮动）。兜底蛋的配色与安装期的矢量蛋同源，呼吸节拍与登录态发光锁步。

头像重生不会使 3D 模型失效——模型只随物种变更或用户显式请求重生；但头像重生会使相册整体失效（身份锚点变了），本地缓存随之清空。

### 1.3 换装系统

换装是实时 3D 渲染的核心优势——不重生模型即可改变伙伴外观。形象由三层几何装配而成：

| 层 | 定义 | 动画跟随方式 |
|----|------|--------------|
| **身体层** | 现有绑定骨骼的 3D 模型（身体 + 骨架 + 表情变形目标），一次性生成、永不因换装重生 | 动画与表情的唯一载体 |
| **服装层** | 独立蒙皮网格，叠在身体外、共享身体骨骼——蓬裙、外套等改变轮廓的服装 | 蒙皮跟随骨骼变形 |
| **挂件层** | 独立网格，挂到某根挂点骨骼——包、帽、眼镜等硬质附件 | 骨骼跟随、不蒙皮 |

四条换装通道：

| 通道 | 形态 | 成本 | 就绪时机 |
|------|------|------|----------|
| **材质预设** | 颜色/粗糙度/金属度覆盖（6 种预设配色） | 零生成 | 立即生效 |
| **AI 纹理** | 全身 PBR 纹理贴图（仅色/材质/图案变化） | 一次生图 | 后台异步生成（数秒），就绪热替 |
| **几何服装** | 独立蒙皮网格（生成几何 + 确定性贴合/蒙皮 + AI 纹理） | 迭代生成 | 后台异步生成（数分钟），就绪装配 |
| **挂件** | 独立网格挂到骨骼挂点（生成几何 + AI 纹理，无蒙皮） | 迭代生成 | 后台异步生成（数分钟），就绪挂载 |

**单一入口**：换装预览只接受描述文本，云端一次分类决定走哪条流水线——"仅改颜色/材质/图案"走纹理（数秒），"改轮廓/新增件"走几何服装，"包/帽/眼镜等硬质附件"走挂件（数分钟）；分类失败默认走能力最全的几何服装路径。

**多件混搭**：每个换装单元声明一个互斥槽位——同槽互斥（新衣顶掉旧衣），异槽并存（上装 + 下装 + 鞋 + 帽 + 包可同时穿戴）。装配顺序按叠放层级升序，贴身衣物先挂。

**布料物理**：蓬裙/斗篷/大衣下摆等"流动"服装由轻量布料求解摆动（腰口锚定随骨骼、下摆自由摆动 + 骨骼球碰撞）；合身衣物由蒙皮逐帧钉住、并对身体表面碰撞推挤。静止态防穿模由生成管线确定性保证，动画期防穿模由客户端运行时身体碰撞保证。

贴图换装覆盖材质或加载新纹理；几何换装加载独立网格、重绑到身体骨骼——两者都是**非破坏性操作**，不动身体模型、骨骼动画和表情。换一件衣服不会打断正在播放的任何状态动画。

---

## 2. 动画状态机

伙伴形象在任何时刻处于一个状态，每个状态驱动一组骨骼动画。本节只锁定**状态切换的归类原则**——这是整个交互系统的骨架。状态枚举与切换规则的实现见 [client/renderer/companion/README.md](client/renderer/companion/README.md)。

### 2.1 四类触发源

区分触发源的关键是**延迟特性**——决定是否需要 fallback 动画：

1. **规则触发（客户端本地，零延迟）**：应用启动/退出、工具调用状态、用户输入起止、语音播放起止、时间。客户端按规则自主决定，不问云端。
2. **语义触发（云端下发，高延迟）**：无法靠规则判定的语义/情绪场景。云端在对话响应中附带情绪语义，客户端收到后切 EMOTIONAL。判断逻辑在云端，客户端只执行 cue。
3. **用户触发（客户端本地，零延迟）**：用户对形象本身的直接操作——戳、拖、悬停。
4. **自主行为（客户端调度，无延迟感）**：空闲下随机插入微动作，纯视觉，不触发语音、不弹气泡（见 §6）。

### 2.2 切换规则（架构级）

- **优先级**（高 → 低）：断连 > 交互 > 工作 > 说话 > 思考 > 倾听 > 情绪 > 睡眠 > 空闲。低优先级状态不可中断高优先级状态（强制选项除外）。
- **情绪/交互是叠加而非抢占**：瞬态播放一次后，回到被它打断的状态。
- **平滑过渡**：骨骼动画交叉淡化（约 250ms 量级），避免硬切。

---

## 3. 空间行为与移动

精灵不是钉在角落的贴纸——它有空间自主性。空间状态管"在哪里、怎么过去"，与动画状态机（§2）正交：动画状态管"在做什么"，空间状态管"在哪里、怎么过去"。

### 3.1 三轴：位置 × 移动方式 × 缩放

- **Locale（场所）**：语义位置——精灵在什么"地方"。
- **Locomotion（移动方式）**：到达目标场所的方式——still / walk / fly / drag。
- **Scale（缩放）**：形象的显示比例——用户设定默认值，运行时按上下文自适应（§3.3）。

任一时刻精灵处于一个场所 + 一种移动方式 + 一个缩放。三个轴与 §2 状态自由组合。

### 3.2 Locales

协议层的**语义场所**共 5 种（云端可下发的语义 cue），另有 1 种客户端内部场所：

| Locale | 语义 | 何时处于 |
|--------|------|----------|
| **home** | 栖息地 | 休息态默认位置（屏幕右下角，可由用户拖拽重设并持久化） |
| **chat** | 对话位 | 对话模式开启（屏幕水平中央上方，对话框锚在下方，§6.1） |
| **perch** | 栖息在某窗口旁 | 用户沉浸工作于某窗口时精灵主动过来"陪"——如趴在 IDE 窗口边缘 |
| **roam** | 漫游中 | 桌面空闲 + 高活跃档位时随机游走 |
| **sleep** | 睡觉点 | 睡眠状态触发后，从 home 移到安静的角落（屏幕边缘/底部） |
| **target**（客户端内部） | 某交互对象旁 | 仪式性行走（§3.6）——精灵"亲手"操作 UI 前先走到目标旁；由工具调用本地触发，**永不来自云端语义 cue**，不属协议场所枚举 |

home 是唯一持久化的场所。其余场所从 home 与上下文派生，是临时位置。

任何场所都可以有"扒边变体"：精灵部分移出屏幕边缘，只露头或上半身往里看——趴在屏幕底边、从侧边探头。

### 3.3 Locomotion、缩放与渲染

移动本身是动画，由 3D 引擎直接驱动——walk/fly 与 idle/speaking 等状态动画同为客户端注入的骨骼关键帧，同等可用。**程序化兜底永远兜底**——模型加载失败时窗口平移 + 变换仍能表达"在动"，零资产依赖。

**缩放**由 3D 引擎直接控制模型缩放，不生成额外资产。范围 0.5×–2×（默认 1×）。两层决定：
- **默认比例**：用户在设置中设定，持久化。
- **运行时自适应**（平滑过渡）：空间紧张时缩小（栖息到窗口旁但边缘空间不够 → 等比例缩到能舒适栖身）；搞怪/情绪时刻放大（特定情绪或高活跃档位下的自主搞怪 → 短暂放大在桌面上蹦跳，情绪结束后平滑回默认）。

### 3.4 四类移动触发源

镜像 §2.1 的触发源分类：

1. **规则触发（客户端本地，零延迟）**：对话模式开启/关闭 → home ↔ chat；onboarding 流程的位置引导；屏幕布局变化（分辨率改变/显示器插拔）→ 重定位 home。
2. **上下文触发（客户端调度，秒级延迟）**：焦点应用切换 → 决定是否换 perch；长空闲 → 移到 sleep；桌面空闲 → 进入 roam。
3. **任务触发（工具调用驱动）**：涉及 UI 对象的工具调用（"帮我打开浏览器"）→ 先走到目标旁，再执行（§3.6）。
4. **用户触发**：拖拽（瞬时，覆盖一切其他移动）；可选"过来"语音/文字指令 → 朝用户当前焦点或屏幕中央走。

### 3.5 打扰档位下的空间策略

空间策略读生效档位（派生规则见 §6.2）：

| 档位 | 允许的空间行为 |
|------|----------------|
| **积极主动** | 全部开放：换 perch、roam、仪式行走、主动找 sleep 点、走过去搭话、放大搞怪 |
| **常规** | home + perch（跟随焦点窗口）+ 仪式行走；**不 roam、不主动换 sleep 点** |
| **保持安静** | **仅 home**（除非用户拖动）；睡眠时移到 home 附近安静角落；不响应焦点/空闲触发的位置切换 |

档位只约束自动空间行为——用户拖拽、"过来"命令等永远生效。

### 3.6 仪式性行走（Ritual Walk）

精灵"动手"做事——打开应用、点击按钮、操作文件——之前先走到目标旁。仪式感来源：精灵的"动手"不是后台调用，而是有空间过程的角色行为。

1. 用户请求涉及某 UI 对象（"帮我打开浏览器"）→ 客户端解析目标位置（窗口/图标/控件）。
2. 精灵从当前位置飞（远距离）或走（近距离）到目标旁，切交互状态。
3. 抵达后播放"操作"动画（点击/触碰），本机执行器同步在目标位置执行点击。
4. 完成后回到原场所或 home。

**不确定性兜底**：找不到目标/目标不可达（屏幕外/被遮挡）→ 精灵人格化表达（"找不到…"/"够不着…"），客户端直接以常规工具调用兜底，不阻塞功能。整个仪式行走通道失败时降级为精灵原地工作——仪式是增强层，不是必要层。

### 3.7 不变量与反模式

**不变量**：
- **移动始终可被用户打断**——用户拖拽或点击时，正在进行的自动移动立即放弃。
- **精灵面部始终在屏内**——精灵可以部分移出屏幕（扒边），但识别度最高的部分（面部）必须始终可见。
- **z-order 永远置顶**——移动中与移动后，精灵窗口永远在其他窗口之上（用户全屏应用除外）。
- **程序化兜底永远兜底**——模型加载失败时窗口平移 + 浮动即可表达移动，不阻塞功能。

**反模式**：
- 不让精灵挡住用户正在用的 UI（perch 选窗口边缘/空隙，不选中央）。
- 不让精灵频繁移动——移动是事件而非常态，常态是"在某处待着"。
- 不让用户看到"瞬移"（调试/全屏切换等场景除外）。

---

## 4. 伙伴生命周期

SpiritAgent 区别于一切既有桌面宠物/桌面 Agent 的核心，在于伙伴有一个**从无到有的诞生过程**，以及诞生后**持续陪伴的长期关系**。这是产品体验的骨架：

```
[安装完成]
    │
    ▼
┌─ 蛋 (Egg) ──────────────────────────────────────────────┐
│  头像产生之前，客户端以"蛋"作为占位形象常驻桌面。        │
│  蛋是产品意象；头像确认后的空挡期由静态精灵相册接棒     │
│  （§1.2 级联），蛋只剩绝对兜底职责。                     │
└───────────────────────┬───────────────────────────────┘
                        │  用户点击/唤醒，进入 onboarding
                        ▼
┌─ 角色定义 (Persona Definition) ────────────────────────┐
│  引导用户描述想要的伙伴：名字、性格、说话风格、外貌与   │
│  视觉风格偏好等。                                       │
│  产出：一份结构化的角色定义，持久化在用户维度。         │
└───────────────────────┬───────────────────────────────┘
                        │  用户确认角色描述
                        ▼
┌─ 头像生成与形象确认 (Avatar) ──────────────────────────┐
│  • 生成半身头像，用户确认或反馈微调                     │
└───────────────────────┬───────────────────────────────┘
                        │  形象 + 音色确认完成
                        ▼
┌─ 3D 建模与孵化 (3D Model Generation & Hatch) ───────────┐
│  • 以画风样图选择 + 正面确认后自动派生的正/左/右/背多视图图生3D 产出 3D 模型         │
│  • 客户端将桌面"蛋"替换为生成的专属形象，播放初次问候   │
└───────────────────────┬───────────────────────────────┘
                        ▼
┌─ 持续陪伴 (Ongoing Companionship) ─────────────────────┐
│  • 伙伴形象常驻桌面（透明置顶窗口）                     │
│  • 伙伴可主动发起交互：问候、提醒、闲聊                 │
│  • 伙伴有长期记忆，随互动积累对用户的了解               │
│  • 伙伴可调用本机能力帮用户做事——叙事上是"伙伴在帮忙"   │
└───────────────────────────────────────────────────────┘
```

诞生的详细交互设计见 §5；空间行为与移动见 §3；持续陪伴的交互范式见 §6。

---

## 5. 初始化与 Onboarding

设计目标：让用户在第一次见面时建立情感连接——**不是"填表"，而是"和一个正在成形的新朋友对话"**。

### 5.1 蛋阶段（Egg）

安装完成 → 蛋以默认形象出现在桌面（透明置顶窗口）。

### 5.2 对话式信息采集（3 子阶段、13 步骨架）

蛋破碎后，开始与用户对话。问题分三个子阶段，**精灵自身的定义先全部确认完，之后才录入用户信息**——中途插入用户题会打断"正在认识这个新朋友"的节奏：

- **角色子阶段**（Q1–Q6）：名字 → 物种 → 角色性别 → 基础形象 → 角色定位 → 性格 → 说话风格。物种/性别/形象描述三步连排形成视觉锚点；说话风格同属精灵属性，与其他角色字段一起在孵化时定稿。
- **音色子阶段**（Q7）：音色偏好（描述句）。排在**形象确认之后**——形象与音色都是"精灵长什么样、听起来什么样"的一部分。
- **用户子阶段**（Q8–Q12）：怎么称呼您 → 您的性别 → 年龄段 → 您的爱好 → 还有什么想告诉我。

每个问题由预制语音说出（同时显示文字气泡），让用户从第一步就建立"它会说话"的预期。全部支持"跳过/返回上一步"；只有名字必填。角色子阶段答完即触发孵化（§5.3），画像生成期间的等待由后续的音色与用户子阶段吸收——把 10–60s 的云端图像生成时间隐藏在用户输入里。

**形象描述**这一步让用户主动告诉伙伴"希望我长什么样"，同时可附一张参考图（图与文字并存，不互斥）：有参考图时让伙伴一次就"照着这张图画自己"，而不是只能在事后推翻重来。

**标签的两种语义**：多数问题的标签就是答案本身，点击即填入输入框；但「我该怎么称呼您？」的标签（名字/昵称/称号/自填）是**称呼类型选择器**——点「昵称」不该把"昵称"二字当成称呼存下来，而是把输入框改问"那，您的昵称是？"再收具体值。

**断点恢复**：每个回答即时落盘单个字段，客户端启动时从下一个未答问题恢复——崩溃/退出不丢进度。**关键差异**：角色子阶段答完即标记"角色已定稿"，但 onboarding 整体只在形象确认且音色 + 用户信息全部答齐后才算"完成"；未确认形象时回到头像确认步，确认后按**音色先于用户信息**路由，中途崩溃后客户端会恢复进入对应子阶段而不是直接跳到结束。

### 5.3 孵化与头像生成

孵化在角色子阶段（Q1–Q6）答完即触发——不等 13 题收齐。说话风格随其他角色字段一同定稿；定稿之后只接受音色与用户信息的增量提交，说话风格的后续修改走设置里的角色管理（§8）。

生图超时/失败时剪影说"我还没想好…"并自动重试（最多 3 次），不暴露技术错误；三次失败后允许稍后再试，不阻断用户。

### 5.4 形象确认（头像）

头像生成完成，剪影散开变为头像展示。操作：**确认** / **重新生成**（可带反馈）/ **自己上传**（上传一张图片作为角色基准形象，重绘为符合契约的头像）。

**两类参考图并存（不互斥）**：基础形象上传的参考图是**身份锚点**——用户最想要的形象，每次重生都携带；头像重生时可额外附一张**风格参考图**，只影响展现形式（构图、风格等），不替换身份锚点。理由：用户最初选择的形象图承载最强烈的意图，后续的图只是在"怎么呈现"上给参考，两者语义不同故应共存而非覆盖。（哪些供应商能同时消费两张参考图属于实现细节，见 backend 模块文档。）

3D 外观由 onboarding 阶段确认的全身立绘（含画风）主导生成：系统首次基于用户上传的参考图（无参考图时使用头像种子图）一次性并发生成多风格正面样图，用户选择一张后可直接确认或进行微调重绘（微调时基于当前选中的参考图重新生成）；正面立绘确认后自动派生正/左/右/背多视图，经**图生3D**提交供应商生成绑定骨骼的 3D 模型；类人面孔（人类/精灵/类人自定义物种）默认精美二次元立绘以规避写实人像在 3D 重建后的恐怖谷（仅作用于 2D 全身立绘生成阶段），非人生物（机甲/灵兽/幻形等）保留写实路线（无恐怖谷问题）。用户可见层（3D 模型、头像与降级精灵图）始终以 GLB 原生 PBR 路径渲染，与身份锚点保持一致。样图与微调正面图在确认前均为草稿：中途退出后重新进入时复用已生成的样图与已选画风、不重复生成；草稿超过保留期才重新生成，确认时草稿才转为正式形象资产。

**形象确认后锁定**：头像确认时点击「确认」即视为锁定形象——此后 3D 模型、头像的重新生成路径对当前用户关闭；物种、性别、基础外貌三个视觉字段不可再改；名字、性格、说话风格、关系定位、换装偏好描述、音色与用户信息仍可编辑。换装与动画生成不受影响。**用户责任约束**：3D 生成成本高，且形象一旦进入模型即永久冻结；onboarding 进入任一锁定字段时顶部横幅直接提示「当前字段是形象确认后无法再次更改的重点内容」，强迫用户在初始引导阶段仔细选择。

引导阶段仅采集基础外貌（描述脸型、体型、标志性细节等不可变特征，确认后锁定）。换装偏好描述**不在引导阶段采集**——3D 外观由基础外貌与选定的全身立绘画风共同决定，最终外观由后期换装系统提供；换装偏好保留为角色可选项，可在「编辑角色」或「重新对话微调」中补充。

形象确认后进入音色子阶段（§5.5），音色确认后才进入用户子阶段——用户在录入用户信息时任何头像重生都不会阻塞 UI。

### 5.5 音色确认

音色子阶段紧接形象确认，包含两步：**先**录入音色描述句（"温柔的少女音、节奏偏慢…"），**后**浏览目录。描述提交后把描述经标签评分映射到当前语音目录中最贴合的声线。目录页形态：

- 顶部**语言页签**（全部 / 中文 / English）
- **推荐卡**（"为你推荐"）：匹配到的音色 + 标签，操作「试听」/「换一个」（在候选里循环）
- **可滚动的完整目录**：点一条即选中并试听
- 底部「使用这个」确认

匹配到的声线由客户端持久化，后续语音透传给引擎。此页**不重复展示已确认的头像**——形象已确认过，再展示一次只会分散注意力。

**二次定制通道**：设置面板 → 伙伴设置 → 音色管理，除目录浏览 + 试听外，还可通过描述生成**专属音色**。

### 5.6 3D 模型生成与最终孵化

形象 + 音色就位，3D 模型异步生成——以用户确认后的正/左/右/背多视图种子图与画风为输入（图生3D），就绪后经事件下发。形象以 idle 动画"活"起来，用确认后的音色说出第一句问候。onboarding 结束，进入持续陪伴。**形象自此由客户端剥夺了对 3D 模型、头像以及物种/性别/基础外貌的修改路径；用户可通过换装或换装偏好调整外观**。

---

## 6. 持续陪伴交互

### 6.1 双模式对话

| 模式 | 触发 | 形态 | 麦克风 |
|------|------|------|--------|
| **对话模式（Chat）** | 点击精灵 / 托盘 / 热键 / 双击 | IM 式气泡（精灵左、用户右），支持文字、粘贴图片、语音条 | 仅录语音条时短暂开启 |
| **语音通话模式（Voice Call）** | 通话按钮 / 右键菜单 | 精灵带"通话中"光环与波形指示，极简 UI + 双向字幕 | 持续开启 |

**位置语义——"对话发生在角色身边"**：休息态精灵在 home（默认屏幕右下角，可拖到任意处）；开启对话模式后，**精灵移到屏幕水平中央上方、对话框锚在下方**，两者在同一中央列紧邻呈现，营造面对面交流；关闭对话 → 精灵回到拖拽位置。语音通话模式不强制居中——精灵留在原位，用户可边干活边说话（ambient 陪伴）。

**对话模式细节**：气泡流精灵在左、用户在右；支持粘贴截图（→ 视觉理解）、拖拽文件。语音条按住录音、松开发送——录音转写为文字后作为普通 prompt 发送。思考中精灵切思考状态 + 气泡区显示省略号。

**语音通话模式细节**：麦克风持续收音，做**半双工分段**——检测到说话（音量超阈值）开始录制，持续静默约 1.3s 判定一句结束 → 转写 → 发送 → 精灵切思考；回复完成后用语音说出，说完回到倾听。**打断**：精灵说话时用户开口（音量超更高阈值）→ 立即止语音、转回倾听。**静默超时**：约 3 分钟无活动自动退出。**双向字幕**（可切换）在精灵下方显示。

**响应模式**（设置项，见 §8）：默认文字（对话模式下精灵以文字应答，不打扰）/ 始终语音（对话模式下精灵也用语音说出回复）。语音通话模式始终语音。主动陪伴消息（§6.2）不受此设置约束。

### 6.2 主动陪伴与打扰档位

云端的定时调度 / 主动消息经推送下发。**伙伴的一切主动行为受三档打扰等级约束**，档位由用户设置 + 客户端检测到的用户活动共同决定，客户端上报当前生效档位。**档位只约束伙伴的主动行为；用户主动发起的交互永远不受限。**

| 档位 | 允许的主动行为 |
|------|----------------|
| **积极主动** | 语音、气泡、主动消息、情绪——全开放 |
| **常规** | 仅轻量气泡/文字消息（无语音）、情绪 |
| **保持安静** | **禁止任何主动消息（语音+文字）**；但推理出的情绪仍可流出，精灵切情绪状态（无气泡无语音） |

**双层档位模型**：
- **手动安静永远不被覆盖**（manual lock-in）。
- **沉浸式/全屏焦点上下文** → 生效档位 = 安静。
- 其他情况 → 生效档位 = 用户偏好。

**消息与情绪是两个独立通道**：保持安静/屏幕锁定时云端静默切断主动消息推送，但推理出的情绪仍经独立事件流出。**断消息不断情绪**——安静档时精灵仍可表达情绪，只是无气泡无语音。

**情境化情绪触发**：用户长时间无活动时，客户端还会主动请求云端做情境化情绪推理（粘人型被冷落的委屈等）——情绪始终由云端推理产出，不退化成客户端规则判断。屏幕锁定同样静默切断主动消息但保留情绪，解锁后静默恢复。

### 6.3 用户直接交互（形象本体）

用户对形象本身的直接操作（不经过对话框）是情绪价值的核心——**形象"有脾气"**：单击戳（高频戳触发递进反应）、双击唤起对话、长按/拖拽（松手回弹）、右键快捷菜单、悬停（注意到鼠标）。

反应由**角色性格**驱动分层：粘人型被戳后撒娇、毒舌型吐槽、管家型礼貌——同一操作不同人格不同反应（客户端据角色定义的性格关键词选反应基调，轻/中/重三层按戳的频率递进）。反应文案与动画从情绪对应的可用变体中挑选。

**智能反应与三大开关**：客户端提供三个独立开关（默认全开）：
- **戳智能反应**：开时优先走云端推理拿到反应，完全替换本地反馈（动画照常，文案与音频替换）；关时回退本地反馈。用户主动触发的推理在客户端与服务端双侧做**5 分钟成本封顶**（同窗口内超出的戳自动回退本地），**但互动统计（戳与对话轮次）永远上报**（夜间可见完整频度分布）。推理失败/超时/限流时静默自动回退本地。拖拽始终使用本地预制反馈（动画 + 性格/通用分组随机语音池），不走云端且不上报互动统计。
- **空闲情境情绪**：控制空闲情境情绪推理的触发；关时直接跳过。
- **自主空间行为智能驱动**：开时由云端同时决定"何时动 + 动什么"（本地规则作为建议性上下文进入判断）；关时由本地规则决策。

### 6.4 自主行为（让形象"活着"）

空闲时形象不是静止贴图。两类自主行为，**都不触发语音、不弹气泡，纯视觉**：

- **微动作**（随机间隔）：眨眼、换重心、看四周、伸懒腰等空闲变体随机切换。3D 引擎经表情（眨眼）+ 骨骼动画变体直接驱动，不走云端。
- **空间自主行为（智能驱动 vs 本地规则）**：智能驱动开关开启时，客户端周期性/事件驱动地向云端提交环境上下文（空闲时长、本地时间、焦点应用、锁屏状态、上次动作间隔），由云端决定是否触发睡觉、漫游、栖息或静止；云端返回"不动"或异常时均保持静止、不做擅自决策。关时回退本地规则（深夜自动睡、专注窗口自动栖身）。

### 6.5 故障态与降级行为（伙伴永不"死"）

伙伴不能"死"、不能弹原始错误框——一切故障以符合人格的方式表达，用户始终觉得"它活着，只是遇到点状况"。故障按脑/手/身隐喻映射到症状，降级状态叠加在 §2 状态机之上、优先级高于常规状态：

| 故障层 | 隐喻 | 精灵表现 |
|--------|------|----------|
| **云端（脑）断连** | 犯困/走神 | 断连：打哈欠、歪头发呆；**久不恢复 → 进入睡眠**，重连后"醒来" |
| **本机执行器（手）宕机** | 手不听使唤 | 对话与陪伴正常；仅当用户要它做事时人格化拒绝 |
| **资产缺失（身）** | 无症状 | 模型不可用时进入静态精灵降级模式（§1.2 级联），用户看到自己的角色静态立绘，完全无感；相册不可用才落蛋形 |
| **3D 生成失败（无 key / 余额耗尽）** | 无症状 | 静态精灵常驻，无错误提示；用户可在设置中配 key/充值后重生模型 |

**断连的分级表达**（避免网络抖动让精灵频繁犯困）：后台断连（用户没在等响应）长宽限才打盹；前台断连（用户刚发消息在等回复）短宽限即表达。重连成功 → 精灵"回神"；**仅在降级曾被表达过时**才补一句，静默降级则静默恢复。

**可执行故障**（需用户做事）——登录失效、麦克风权限缺失等：精灵用人格化方式说出问题，并嵌入一个明确的可操作提示（"逃生口"）。语音不可用 → 转纯文字应答；工具调用失败 → 诚实但人格化报告（"我试了但没搞定…"，不暴露原始错误，诚实是为了信任）。**开发者模式**（Ctrl+Shift+D）：原始协议帧对开发者可见，生产永远人格化。

### 6.6 拟人化交互节奏

真实的聊天不是「每条消息都换一段文字回复」——伙伴有时只回一个表情或动作，有时连说几句，用户也常连发几条。设计上把「回复」拆成**可独立触发的通道**，由 LLM 声明的结构化意图驱动，而不是把每一轮都硬编码成「文字气泡 + 语音」。精确的标签语法见 [PROTOCOL.md §1.4](../PROTOCOL.md)。

**四条通道，彼此解耦**：

| 通道 | 触发 | 表现 |
|------|------|------|
| **情绪** | LLM 声明情绪意图 | 3D 精灵播情绪肢体动画 + 聊天窗左栏精灵头像切换为对应表情头像（未就绪/失败回退形象头像）（无气泡、无语音） |
| **动作** | LLM 声明动作意图 | 3D 精灵播动作动画（可叠加在情绪上） |
| **文字** | LLM 输出正文 | 气泡 +（语音模式下）TTS |
| **空间** | LLM 声明场所意图 | 精灵落位/漫游/栖息 |

三条拟人化场景是这四条通道的组合：

1. **纯动作/情绪反应**：LLM 只声明情绪（可带动作）而不输出正文——精灵只做 3D 表情/动作，不弹气泡、不发声。这类反应**仍会作为一条「情绪痕迹」写进对话历史**（渲染成弱化的提示行、而非文字气泡）：保证下一轮 LLM 的上下文完整，伙伴记得自己刚才撅了嘴/做了某个动作，用户追问「还在生气吗」时才有据可依。
2. **伙伴连发多条**：LLM 在一回合内连发多条短回复，用分隔行隔开；渲染端收尾当前气泡、短暂停顿后再显示下一条（数量不限；分段只影响呈现，历史里仍是同一条回复）。主动消息的「要不要追问、等多久再追问」由 LLM 自行决定，而非固定时长。
3. **用户连发消息**：用户快速连发多条时，本地先合并——一个约 **4 秒的窗口**内把多条消息作为一批一次性提交，只触发一次 LLM 调用；这批消息在上下文里按相邻用户角色折叠。**这 4 秒是刻意的合并窗口，不是发送延迟**；窗口结束、新一轮开始或用户主动停止时立即提交。

**可用的动作/情绪是喂给 LLM 的、不是让 LLM 猜的**：系统提示注入「内置动画 ∪ 模型生成动画」的完整动作清单与「内置情绪 ∪ 自定义表情」清单。找不到合适项时，LLM 可实时创造新的表情或动作——新表情注册进情绪清单并在后台预热生成表情头像图，新动作落库后热绑定到 3D，均无需重启。

---

## 7. 语音交互

- **语音输出**：默认**云端优先**
- **语音输入**：默认**本地优先**
- **始终可回退文字**：任一语音环节失败不阻断交互——语音失败仅显示文字，听写失败提示"没听清，用打字吧"。

具体引擎选型、供应商列表、降级开关、录音时长配置见 [backend/README.md](backend/README.md) 与 [runner/README.md](runner/README.md)。

---

## 8. 设置与个性化

两类设置分处两个窗口，由**网关可用性**决定归属：

- **伙伴设置**（精灵窗口，网关可用）：右键精灵 → 伙伴设置。形象确认后，**「形象」与「3D 模型重新生成」入口直接移除**；仅保留「换装」（浏览预设配色 + 已生成纹理，点击即换）。其余模块包括角色管理（**两套路径**：「编辑角色」表单式直接改 4 个可编辑字段（名字/关系定位/性格/换装偏好），**或**「重新对话微调性格」5 步对话式向导改可编辑字段含用户信息——向导单次保存收尾，**保留既有长期记忆**，不重置完成状态；客户端保存时自动剥离物种/性别/基础外貌三个锁定字段）、响应模式（默认文字 / 始终语音）、打扰档位（显示用户偏好，生效状态由活动感知覆盖，UI 不直接显示覆盖）、音色管理（目录切换 + 试听）、形象缩放（默认比例 0.5×–2×）。
- **应用设置**（托盘 → Settings...，独立工具窗口，无网关）：账户、语音（听写开关 / 降级开关 / 录音时长 / 引擎）、音色目录（只读浏览 + 试听）、本机执行器、技能等通用配置。

> **设计约束**：语音目录、形象、onboarding 等依赖网关的伙伴设置必须住在精灵窗口；工具窗口不启动网关，只走通用接口（音色目录页是网关目录的镜像，专为工具窗口而设）。
