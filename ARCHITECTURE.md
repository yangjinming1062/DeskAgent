# SpiritAgent 架构设计

> **面向人类与 Agent 的系统总览。读代码前先读本文。**

SpiritAgent 是一个**根据用户描述定制的、具有专属形象的陪伴型桌面伙伴**。用户首次安装后经一段 onboarding 对话定制伙伴的名字、性格、说话风格与外貌，系统据此生成专属桌面形象；此后伙伴常驻桌面、主动陪伴，并调用本机能力帮用户做事。

三个关键词决定一切产品与技术取舍：**定制**（形象与人格由用户定义并生成）、**陪伴**（主动、持续、有记忆的关系，而非一次性问答）、**伙伴**（交互对象是"他/她/它"，工具能力只是伙伴"会做的事"，不是产品的主角）。

**文档分层**：
- 产品设计意图（形象、动画、生命周期、onboarding、交互范式）见 [DESIGN.md](DESIGN.md)。
- 跨模块协议契约（JSON-RPC 方法 / 枚举 / 事件 / 安全 / 凭据）见 [PROTOCOL.md](PROTOCOL.md)。
- 本文聚焦**系统物理架构**——模块边界、为什么必须这样分离、通信链路的形态与权衡、跨模块不变量。
- 模块实现细节、文件树、配置项见各模块 [README.md](README.md)（backend / client / runner / installer）。

---

## 1. 三模块物理解耦

系统在物理上拆分为三个核心子模块，各自承载不同职责、运行在不同环境、由不同信任域管辖：

| 层 | 所在 | 承载 | 为什么必须在物理上分离 |
|----|------|------|------------------------|
| **大脑** | Backend（云端） | LLM 编排、伙伴人格（角色定义 + 长期记忆）、形象资产生成、云端工具 | 伙伴的"性格与记忆"是跨设备、需多用户隔离、且依赖大模型与生图算力的核心资产，必须云端托管 |
| **手脚** | Runner（本地） | 终端、文件、浏览器、代码沙箱等本机操作能力 | 伙伴"帮用户做事"必须真正触碰用户本机；但本机操作不可上云，且执行环境必须与凭证隔离 |
| **形象与枢纽** | Client（本地） | 桌面精灵形象渲染、陪伴式交互、凭证持有、WS 中转、Runner 生命周期 | 伙伴要在用户桌面常驻可见、能听能说；同时是唯一可信的凭证持有点与本地/云端流量中转点 |

**核心不变量**：云端永不接触用户本机与凭证，本地 Runner 零凭证运行，Client 是唯一的可信枢纽。即便伙伴人格被 prompt 注入攻陷，最坏情况也只是受限用户账户下的本地工具被调用，不会泄露云端凭证或影响其他用户。

伙伴的"人格"由云端持久化的**角色定义**驱动（注入每次对话的系统提示词）；伙伴的"形象"由云端生成的**形象资产**驱动（由 Client 拉取并渲染）。两者共同构成"这一个伙伴"的数字身份，归属于该用户。

---

## 2. 架构拓扑

    ┌─────────────────────────────────────────────────────────────┐
    │                        Backend (web)                        │
    │                   (云端大脑 / FastAPI)                        │
    │  - 伙伴人格：角色定义持久化、长期/短期记忆管理                │
    │  - 专属形象资产生成编排（portrait + 3D 模型 + 表情头像）  │
    │  - LLM 编排与系统提示词装配（角色定义注入每次对话）           │
    │  - 云端工具执行（Web 搜索、语音、生图、主动消息）            │
    │  - 事件发布 / Cron 调度中转（Outbox，主动陪伴的基石）        │
    └─────────────────────────┬───────────────────────────────────┘
                              │ WebSocket 长连接（带用户 JWT 鉴权）
                              │ /api/chat/ws
    ┌─────────────────────────▼───────────────────────────────────┐
    │                       Client                               │
    │              (桌面伙伴形象载体 + 本地枢纽 / Electron)          │
    │  - 桌面精灵 3D 实时渲染与陪伴式交互                          │
    │  - onboarding：角色定义引导、portrait + 3D 模型拉取与孵化过渡 │
    │  - 登录鉴权与用户凭证加密落盘                                │
    │  - 本地 IPC 服务端与 Runner 进程生命周期管理                 │
    │  - 双向工具调用路由及反向 RPC 代理中转                       │
    │  - 统一自更新（Electron 二进制 + Runner wheel）               │
    └─────────────────────────┬───────────────────────────────────┘
                              │ 本地 OS IPC（Windows 命名管道 / macOS UDS）
                              │ WebSocket 帧 + 每次启动握手 token
    ┌─────────────────────────▼───────────────────────────────────┐
    │                        Runner                               │
    │            (本地手脚 / uv build wheel)                       │
    │  - 本地环境执行器（PTY 终端、文件读写、代码沙箱）            │
    │  - 动态 MCP 协议客户端                                      │
    │  - 浏览器多后端控制                                        │
    │  - 自动化安全审查拦截器                                      │
    └─────────────────────────────────────────────────────────────┘

并行的 **Installer（安装器 / Tauri 2）** 在首次安装时于本地引导 uv Python 运行时，解压释放 Runner wheel、默认配置与基础 Skills 到本地平台标准路径下；首装完成即进入"蛋"阶段（[DESIGN.md §5](DESIGN.md)）。

> **Client 的定位是双层叠加**：底层是可信枢纽（凭证、中转、Runner 编排、自更新——这部分是 backend/runner 复用所依赖的不变契约），上层是桌面伙伴形象与陪伴交互。两层共享同一个 Electron 主进程，但职责严格分离：枢纽层处理协议与安全，伙伴层处理形象渲染与用户体验。

---

## 3. 职责矩阵

| 维度 | Backend（云端大脑） | Client（伙伴载体 + 本地枢纽） | Runner（本地手脚） |
|---|---|---|---|
| **运行环境** | 容器/云端服务器（Linux Docker） | 用户本机原生（Win/macOS） | 本地静默进程（venv Python 运行时） |
| **状态持有** | 伙伴角色定义、形象资产、长期记忆、PostgreSQL、LLM API 凭证、会话历史 | 用户身份凭证（加密）、Runner 进程句柄、本地工具集 Schema 缓存、形象资产本地缓存 | 终端环境快照、浏览器会话、本地 MCP Server 句柄 |
| **核心职责** | 接收用户消息、装配角色定义 + 记忆上下文、流式调度 LLM、按需生成/再生形象资产、解析工具调用并路由 | 3D 实时渲染桌面伙伴形象、承载陪伴式交互、引导 onboarding；维护本地安全防线、中转工具帧、代理 Runner 的反向 LLM 请求 | 纯粹执行底层工具逻辑，上报真实可用的工具 Schema 列表 |
| **安全准则** | 不可信输入包裹、保留键覆盖保护、脱敏日志、角色定义属用户隐私数据 | 渲染进程隐藏凭证、仅暴露本地文件系统代理拦截 | 危险命令阻断、路径不敏感写限制、SSRF 防护 |

> 模块实现细节（依赖方向、文件树、供应商协议对比、错误分类管道等）见各模块 [README.md](README.md)。

---

## 4. 通信形态与权衡

全链路采用 **JSON-RPC 2.0** 封装双向流量。所有方法枚举、事件类型、错误信封、asset 签名见 [PROTOCOL.md](PROTOCOL.md)。

### 4.1 协议链路映射

#### A. Backend ↔ Client

单一 WebSocket 通道 /api/chat/ws?token=<jwt>，承载所有流式对话、控制事件、工具触发，以及形象资产下发与 onboarding 控制信令。**普通 chat / tool 类方法**见 [backend/README.md](backend/README.md)；**伙伴生命周期专用的扩展方法**见 [PROTOCOL.md §1.2](PROTOCOL.md)。

#### B. Client ↔ Runner

本地 OS IPC：Windows 命名管道 / macOS UDS（0600），承载 WebSocket 帧协议。Client 充当 RPC Server，Runner 启动时主动连入；upgrade 携带每次启动生成的握手 token（校验失败回 401，握手不完成）。**选用 OS IPC + WebSocket 帧（而非 stdio 重定向）的权衡**：
1. 避免 C 库底层日志或 Python print 污染标准输入输出帧。
2. 全双工并发，按 id 异步匹配响应，避免进程读写阻塞与全局锁。
3. 零端口监听面：端点路径与 token 由 Client 单向下发，Runner 重连间重读文件以跟随 Client 重启；Windows 管道侧 token 为实际准入闸门，macOS 侧 0600 socket 为主闸门。

完整方法清单与 capabilities payload 定义见 [PROTOCOL.md §2](PROTOCOL.md)。

### 4.2 核心数据流（架构级）

#### I. 标准工具调用流（伙伴"帮忙"的底层通道）

    LLM 产出工具调用
        │
        ▼
    Backend 编排（拦截保留字段、检查迭代预算、创建调用关联）
        ▼  WS 事件下发（tool.call）
    Client 路由中转
        ▼  本地 IPC（execute_tool）
    Runner 执行（安全扫描 → 本地执行 → 脱敏清洗）
        ▼  本地 IPC 响应
    Client 捕获连接异常（Runner 离线快速失败）
        ▼  WS 回传（tool.result）
    Backend 匹配调用关联，唤醒对话协程
        ▼
    LLM 收到工具结果继续

> **产品不变量**：工具调用在用户侧以"伙伴在帮忙"的叙事呈现，原始 tool.call / tool.result 帧不直接暴露给最终用户（开发者视图除外）。

#### II. 反向 RPC 流（Runner 借大脑）

本地 Runner 工具需要借助大模型能力，但**不持有任何云端凭证**，通过 Client 代理转发：

    Runner ──(本地 WS: request_llm)──> Client ──(HTTP POST: /api/llm/completion)──> Backend ──> LLM
                                            │
                                    (Client JWT 鉴权)

**速率守卫**：Client 转发前统计单会话请求次数与载荷大小（硬上限 200 帧 / 1MB），防止 Runner 工具逻辑失控刷爆 LLM 额度。方法级契约见 [PROTOCOL.md §3](PROTOCOL.md)。

#### III. 本地文件系统代理拦截

受限于沙箱或 Docker 容器无法看到的本地环境信息，客户端在渲染层发出 WS 请求前先行拦截处理（具体实现见 [client/README.md](client/README.md)）：项目根目录恢复、@ 路径补全等本地信息不上报云端。

#### IV. 伙伴表达事件流（情绪 + 动画资产）

伙伴"如何表达"由 Backend 产出语义、Client 负责渲染，两者经 WS 解耦。情绪 cue 与动画资产是两条独立的流，互不阻塞：

- **inline affect**：情绪 cue 随其所属话语在同一响应帧下发，Client 无需二次猜测"这句话该配什么情绪"。
- **语义/渲染解耦**：Backend 只产出情绪 + 可选场所语义，绝不指定渲染方式或像素坐标。客户端据 3D 模型可用动画与表情决定渲染（情绪的渲染分工：3D 面部仅眨眼/口型，情绪面部由聊天窗表情头像换图承载）。这条解耦使模型加载不影响语义层（详见 §6.3）。

3D 模型生成流（图生3D 供应商 → 正面/侧面/背面多视图种子图导入，显式选择不自动回退；产物形态由供应商云端能力 + SPEC 校验门严格守住；链拓扑与失败语义详见 [docs/PIPELINE.md](docs/PIPELINE.md)）的**协议契约**（事件 payload、供应商标识、降级层级）见 [PROTOCOL.md §1](PROTOCOL.md)。

---

## 5. 主动陪伴与事件机制（Cron + Outbox）

**陪伴区别于工具的关键，在于伙伴会主动找用户。** 系统以 PostgreSQL LISTEN/NOTIFY + 事件 outbox 表支撑毫秒级、可水平扩展的事件分发，使伙伴的主动问候、定时提醒、情境化闲聊无需轮询即可送达：

1. **写出事件**：业务或 Cron 协程将待发送的 WS 帧写入事件 outbox 表。
2. **数据库触发器**：PostgreSQL STATEMENT 级触发器在 INSERT 时自动发出 NOTIFY。
3. **副本认领（Atomic Claim）**：每个 Backend 副本独立 LISTEN，收到唤醒后原子认领并消费该行。调度器 tick 只写入事件不 await WS 发射，避免慢客户端拖垮事务。

主动消息、Cron 定时、形象/角色变更通知等所有"伙伴主动行为"都经此通道下发。通道还承载一种**内部事件**（cron.turn.request）：调度器 tick 副本不执行自主回合（回合的流式输出、工具关联、运行时会话都是进程内状态），只写一行 outbox，由认领循环按"持有该用户 WS 的副本"路由执行。实现细节见 [backend/README.md](backend/README.md)。

### 5.1 打扰档位约束

所有"伙伴主动行为"受三档打扰等级约束（积极主动 / 常规 / 保持安静），档位由用户设置 + 客户端检测到的用户活动共同决定，客户端经 companion.set_disturbance_tier 上报。产品语义见 [DESIGN.md §6.2](DESIGN.md)；此处只锁定架构不变量：

- **Client 是档位的唯一权威**：它持有用户偏好 + 活动上下文，独立计算生效值（应用「手动安静永远不被覆盖」+ 沉浸式/全屏 → 安静规则），并把最终结果单向推给 Backend。
- **Backend 只持久化生效值**：供服务端闸门（主动消息、cron 自主回合）使用——既不独立推导，也不存储用户偏好或焦点上下文；重启不丢档位。
- **消息与情绪是两个独立通道**：保持安静 / 屏幕锁定时 Backend 静默切断主动消息推送，但 LLM 推理出的情绪经独立事件流出——即**断消息不断情绪**。

### 5.2 情境化情绪

用户长时间无活动时，客户端跨过阈值时请求后端加载角色定义 + 最近记忆跑一次 LLM 推理，决定是否发出情境化情绪。**触发时机由客户端控制（知道真实 idle 状态），情绪推理由后端承担（有角色定义 + 记忆）**——各取所长，客户端不退化成规则驱动的文案池。

### 5.3 副本语义

进程内状态决定了两类进程的可扩展性不同：

- **后端 web 进程（chat 亲和，单副本语义）**：WS 连接、运行时 chat 会话、IPC 关联表都在进程内存，一个用户的对话回合必须落在持有其连接的副本上——按单副本设计部署。跨副本的部分已外置：打扰档位落库，cron 自主回合经 outbox 行路由到持连副本。

---

## 6. 角色定义与表达层契约

Backend ↔ Client 之间的核心契约：角色定义如何驱动伙伴、形象资产如何生成与下发、情绪如何表达。**所有枚举（emotion / locale）、事件类型、asset 签名 TTL、错误信封**见 [PROTOCOL.md §1](PROTOCOL.md)。

### 6.1 角色定义（唯一真相源）

onboarding 产出的结构化角色定义持久化在 Backend 用户维度，作为系统提示词的一部分注入该用户的每次对话，驱动伙伴的说话风格、性格表现与主动行为倾向。角色定义是伙伴行为的**唯一真相源**：变更只能由用户显式发起（重新进入角色编辑），禁止 LLM 自行改写。

### 6.2 形象资产（跨模块契约）

伙伴的视觉表达由 portrait、表情头像、3D 模型三层资产构成，均归属用户、在用户维度持久化。资产体系的产品形态与用途见 [DESIGN.md §1](DESIGN.md)；此处只锁定跨模块契约：

- **资产归属用户维度**，不跨用户共享、不下发到他租户。
- **半身像（portrait）**：聚焦头部细节的写实肖像是用户可见的身份锚点，也是图生3D 全身立绘参考、表情头像与精灵相册的共同输入；硬性要求纯白平面背景（渲染依赖）。
- **表情头像按情绪标签懒生成**：以半身像为身份参考生成的头像特写，按 (用户, 情绪, 头像) 缓存复用、未命中才生成；情绪标签来自协议枚举 ∪ 自创情绪注册表，生成语义后端权威（契约见 [PROTOCOL.md §1.4/§1.5](PROTOCOL.md)）。
- **prompt 增强是读取型操作**——角色定义不被 LLM 改写，LLM 异常向上传播。
- **3D 模型由云端图生3D 供应商生成**：以确认后的正/左/右/背多视图种子图作为输入提交供应商生成模型；绑骨、动画绑定、面部变形目标、产物归一化、几何压缩（5–10× 体积缩减）全部由供应商在云端链路内完成（链拓扑与不变约束见 [docs/PIPELINE.md §1](docs/PIPELINE.md)）。**动画全部由供应商烘焙进 GLB**——客户端不持有任何 clip 库或供应商命名；供应商随模型下发该次生成的「语义键 → 预设 token」映射，客户端按三级降级兑现为 GLB 内真实存在的 clip（兑现落空时角色停在绑定姿势，符合 §10「永不空白」不变量）。未配置任何图生3D 供应商时后端不生成 3D 模型，客户端按 §10 层级以静态精灵交互；生成失败时客户端渲染程序化蛋形兜底角色。
- **资产传输与内容寻址本地缓存**：服务端模型/资产接口支持断点续传与 ETag；Client 按内容哈希持久化到本地缓存目录，无网络开销秒级命中。契约见 [PROTOCOL.md §1.5](PROTOCOL.md)。
- **受控再生成**：形象在多次会话间保持稳定。变更只在用户主动要求时发生（重生 portrait / 重生模型）。
- **静态精灵相册是 3D 的同级后备而非装饰**：覆盖无 key / 余额耗尽 / 生成失败与模型重生成、换模空挡期。相册按用户维度存储、按头像失效（头像重生即整体作废重生成）。

### 6.3 表达层契约

伙伴"说什么"由 LLM 产出，"怎么动、什么情绪"由 Client 渲染。**完整 emotion / locale 枚举、解析路径**见 [PROTOCOL.md §1.4](PROTOCOL.md)。

核心契约：
- **情绪 cue（affect）**：后端在对话响应/主动消息中携带情绪语义字段，客户端据此驱动动画状态机与表情头像。emotion 枚举由 Backend 白名单与客户端情绪映射**双端对齐**，未覆盖的情绪一律按中性处理。
- **空间 cue（spatial）**：后端可附带可选**场所**（home / chat / perch / roam / sleep，协议枚举共 5 项）与可选目标关键字。**后端不产出像素坐标**——客户端据场所 + 当前空间状态决定最终位置与移动方式。target（仪式行走的目的地）是**客户端内部场所**，由工具调用本地触发、不在协议枚举内（见 [DESIGN.md §3.2](DESIGN.md)）。
- **语义与渲染解耦**：后端只产出语义，绝不指定渲染方式或具体坐标。客户端决定渲染——情绪驱动肢体 clip（按当前状态的语义键在「语义键 → 预设 token」映射中兑现为 GLB 内真实存在的 clip，缺键或兑现落空回退 idle）+ 聊天窗表情头像换图（未就绪/失败回退半身像）；3D 面部仅眨眼/口型。这使模型加载与语义层互不阻塞。
- **affect 继承角色定义的抗注入保证**：affect 由已注入角色定义的 LLM 产出，自然符合人格；角色定义本身受保留键与不可信包裹机制保护，affect 因此继承同一保证，无需额外的情绪过滤层。
- **affect 与文本同帧，语音由客户端拉取式合成**：对话响应帧内联 {文本, 情绪}，不内联语音音频。客户端收到后先据情绪切情绪状态，再据文本拉语音切说话状态，保证情绪基调先于语音进入。
- **用户画像自动召回**：结构化用户字段（称呼/性别/年龄段/爱好/自由文本）在对话入口渲染成片段注入系统提示词稳定段，紧跟角色定义之后——LLM 每个 turn 都能直接看到用户身份事实，不依赖记忆工具调用。

### 6.4 行为驱动的双层模型

伙伴"做什么、怎么做"由两个独立来源驱动，两者不可混淆、也不可错配：

- **角色定义（静态，用户定义）→ 视觉身份与资产生成**：半身像的生图提示词从角色定义装配。换风格 = 改角色定义 → 重生半身像 + 再生纹理。3D 模型按用户显式请求重生，不随半身像重生。
- **记忆（动态，随互动累积）→ 运行时行为表达**：记忆注入 LLM 上下文，驱动**说什么、表现什么情绪、何时主动搭话、主动频率**。用户随口表达的偏好写入记忆后，LLM 在后续交互中更频繁地产出对应情绪。

**核心不变量**：记忆驱动的个性化通过**运行时行为**实现，**不通过模型重生实现**。模型只随物种变更或用户显式请求重生。这避免了"每学到一条新偏好就重生成全部模型"的不可行开销，同时让伙伴随关系深入而"表现得不一样"。

---

## 7. 安全与准入控制（架构原则）

**物理与凭证隔离**是安全设计的根基：

- **大脑**（云端）不接触用户本地操作系统；即便 prompt 注入攻陷伙伴人格，也在受限用户账户下执行本地工具。
- **手脚**（Runner）零 Token 运行，无法越权请求 Backend 管理级 API。
- **枢纽**（Client）通过 Electron 主进程 safeStorage 将激活码加密落盘。渲染与预加载进程无法接触 safeStorage 接口，阻断 XSS 窃取凭证（跨平台：Windows DPAPI / macOS Keychain）。
- **用户隐私数据**：角色定义与形象资产归属于该用户，按用户维度隔离。

**防御纵深（Defense Layers）**四层，完整契约见 [PROTOCOL.md §5](PROTOCOL.md)：

1. **输入清洗与指令包围**：不可信文本强制包裹、保留键防注入、角色定义防篡改。
2. **本地执行拦截**：硬阻断（系统级/凭证文件）+ 软防线（跨域写需显式许可）。
3. **动态扫描与防护**：shell 命令前置静态审查、SSRF 建连双重校验。
4. **凭据边界**：API key 永不离后端（指纹化）、自更新签名、Skills 走单独通道。

---

## 8. 全局开发不变量 (Invariants)

**架构层（不可破坏）：**

1. **Runner 零凭证**：Runner 进程不能被授信任何 Backend Token，所有出站/LLM 请求必须向上借道 Client 中转代理。
2. **错误遮蔽**：抛出到前端的内部错误信息必须脱敏清洗，严禁包含数据库账号、服务器本地路径等栈帧细节（信封契约见 [PROTOCOL.md §1.6](PROTOCOL.md)）。
3. **两阶段更新强校验**：禁止绕过 Stage 1 的预取逻辑直接覆盖本地 Runner 文件；公钥签名不匹配的升级包在 Staging 阶段直接拦截（签名契约见 [PROTOCOL.md §5.5](PROTOCOL.md)）。
4. **ID 职责分立**：session_id 就是 conversation_id 的字符串形式（跨 WS 重连稳定、与会话同生命周期）；Client 侧用字符串、Backend 侧持久化为整型。call_id 作为唯一 Future Key 标识单次 RPC 生命周期（详见 [PROTOCOL.md §6](PROTOCOL.md)）。
5. **平台兼容性防御**：Windows 下进程控制或路径读取必须采用 Windows 原生封装，防止 Windows PTY 进程链悬挂（具体封装见 [runner/README.md](runner/README.md)）。
6. **副本边界**：web 进程因运行时 chat 会话与 IPC 关联的进程内状态保持单副本语义（跨副本状态经持久化与 outbox 路由外置）。

**产品层（伙伴体验的底线）：**

7. **角色定义是伙伴行为的唯一真相源**：伙伴的所有输出风格、主动行为受持久化的角色定义约束；角色定义只能由用户显式发起变更，禁止 LLM 自行改写。
8. **形象资产归属用户**：生成的专属形象归属该用户，按用户维度隔离，不跨用户共享；形象在多次会话间保持一致，除非用户触发受控再生成。
9. **陪伴优先于工具**：产品决策发生冲突时，陪伴体验优先于工具效率。工具调用以"伙伴在帮忙"的叙事呈现，原始协议帧不直接暴露给最终用户（开发者视图除外）。
10. **伙伴表达永不空白**：任何动画状态或情绪 cue 无对应就绪资产时，客户端必须回退 idle 动画或降级渲染，用户永远不可见"该动画尚未加载"的空白或加载态。渲染兜底按「3D 模型 → 静态精灵相册 → 程序化蛋形」层级降级。

**安全层：**

11. **LLM 工具执行边界**：后端只做 LLM 编排,不派生任何 `eval` / `exec` / subprocess / Docker 容器执行 LLM 生成的代码。3D 模型质量完全落在供应商图生3D API 上,后端仅做 SPEC 校验门。

---

## 9. 子模块详细 README 开发索引

模块级行为契约、文件树、跨文件设计权衡分别记录在各子目录 README 中：

- **Backend（云端大脑）**：角色定义与形象资产的数据模型、生图 prompt 装配、记忆管理、LLM 编排——[backend/README.md](backend/README.md)
- **Runner（本地手脚）**：执行器与工具库、终端环境后端、浏览器多后端——[runner/README.md](runner/README.md)
- **Client（伙伴载体 + 本地枢纽）**：3D 实时渲染引擎、骨骼动画 + 眨眼/口型、表情头像、onboarding/孵化流程、自更新——[client/README.md](client/README.md)
- **Installer（安装器）**：引导协议、Python 运行时分发、首装进入"蛋"阶段——[installer/README.md](installer/README.md)
- **Scripts（发布与集成）**：构建链与导入规范检查——[scripts/README.md](scripts/README.md)
