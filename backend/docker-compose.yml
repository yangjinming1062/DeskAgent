services:
  postgres:
    image: postgres:15
    container_name: deskagent-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: deskagent
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 3s
      retries: 5

  backend:
    build:
      context: ..
      dockerfile: backend/Dockerfile
    container_name: deskagent-backend
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    # All config lives in backend/.env (copy .env.example → .env and edit).
    # Only DATABASE_URL is overridden here because the container must reach
    # postgres by service name, not localhost.
    env_file:
      - .env
    environment:
      DATABASE_URL: postgresql+psycopg://postgres:postgres@postgres:5432/deskagent
    ports:
      - "8000:8000"
    volumes:
      - ./updates:/app/backend/updates

volumes:
  postgres_data:
